.page-header
  .pull-right
    = link_to [:new, :admin, :page], class: 'btn btn-link' do
      %i.icon-plus.text-success
      %span.text-success
        Add
  %h1 Pages
- if @pages.empty?
  %p.no-record
    No records to display
- else
  = render @pages
